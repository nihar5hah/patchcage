[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "patchcage-flask-sql-injection-demo"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["flask"]

[tool.pytest.ini_options]
testpaths = ["tests"]
